[build]
  base = "poc-app"
  command = "npm install && npm run build"
  publish = "poc-app/.next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Disable submodule checkout to avoid the error
[git]
  submodules = false
  fetch-depth = 1
